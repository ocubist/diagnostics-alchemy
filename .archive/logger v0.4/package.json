{
  "name": "@ocubist/logger",
  "version": "0.4.11",
  "description": "A custom logger based on pino for Node.js applications.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "new-project": "npm install zod && npm install --save-dev @types/jest jest rimraf ts-jest ts-node typescript",
    "build": "rimraf dist && tsc",
    "publishProject": "npm run build && npm publish --access public",
    "test": "jest --config jest.config.js",
    "test:watch": "jest --config jest.config.js --watch",
    "clean-install": "rimraf node_modules dist && npm install && npm run build",
    "install-latest": "npm install @ocubist/file-stream-manager@latest @ocubist/singleton-manager@latest @ocubist/utils@latest"
  },
  "keywords": [],
  "author": "ocubist",
  "license": "MIT",
  "dependencies": {
    "@ocubist/file-stream-manager": "^0.3.2",
    "@ocubist/singleton-manager": "^0.4.2",
    "@ocubist/utils": "^0.4.2",
    "lodash": "^4.17.21",
    "pino": "^8.16.2",
    "rimraf": "^5.0.5",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/lodash": "^4.14.202",
    "@types/node": "^20.10.3",
    "@types/pino": "^7.0.5",
    "ts-node": "^10.9.1",
    "typescript": "^5.3.3"
  }
}
